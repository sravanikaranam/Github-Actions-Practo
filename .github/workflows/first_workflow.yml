name: Basic workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Greet
              run: echo "Hi,  What are the files you have?"
         
            - name: List files
              run: ls -ltra

